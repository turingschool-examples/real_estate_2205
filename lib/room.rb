class Room
attr_reader :category, :area, :length, :width
  def initialize(category, length, width)
    @category = category
    @area = area
  end
  def multiply(length, width)
    room_1.area
    room_2.area

  end
end
